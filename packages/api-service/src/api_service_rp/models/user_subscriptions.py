"""
RP Strength Training API

RP Strength Training API for workout data, user profiles, subscriptions, and training management.

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from datetime import datetime
from typing import Any, ClassVar, Self

from pydantic import BaseModel, ConfigDict, Field, StrictStr

from api_service_rp.models.active_subscription import ActiveSubscription
from api_service_rp.models.consumed_iap import ConsumedIap


class UserSubscriptions(BaseModel):
    """
    UserSubscriptions
    """  # noqa: E501

    active_subscriptions: list[ActiveSubscription] | None = Field(
        default=None, alias="activeSubscriptions"
    )
    consumed_iaps: list[ConsumedIap] | None = Field(default=None, alias="consumedIaps")
    stripe_ids: list[StrictStr] | None = Field(default=None, alias="stripeIds")
    training_last_access_added_at: datetime | None = Field(
        default=None, alias="trainingLastAccessAddedAt"
    )
    __properties: ClassVar[list[str]] = [
        "activeSubscriptions",
        "consumedIaps",
        "stripeIds",
        "trainingLastAccessAddedAt",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self | None:
        """Create an instance of UserSubscriptions from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in active_subscriptions (list)
        _items = []
        if self.active_subscriptions:
            for _item_active_subscriptions in self.active_subscriptions:
                if _item_active_subscriptions:
                    _items.append(_item_active_subscriptions.to_dict())
            _dict["activeSubscriptions"] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in consumed_iaps (list)
        _items = []
        if self.consumed_iaps:
            for _item_consumed_iaps in self.consumed_iaps:
                if _item_consumed_iaps:
                    _items.append(_item_consumed_iaps.to_dict())
            _dict["consumedIaps"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: dict[str, Any] | None) -> Self | None:
        """Create an instance of UserSubscriptions from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "activeSubscriptions": [
                    ActiveSubscription.from_dict(_item)
                    for _item in obj["activeSubscriptions"]
                ]
                if obj.get("activeSubscriptions") is not None
                else None,
                "consumedIaps": [
                    ConsumedIap.from_dict(_item) for _item in obj["consumedIaps"]
                ]
                if obj.get("consumedIaps") is not None
                else None,
                "stripeIds": obj.get("stripeIds"),
                "trainingLastAccessAddedAt": obj.get("trainingLastAccessAddedAt"),
            }
        )
        return _obj
